---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: cert-manager
  namespace: flux-system
spec:
  interval: 5m0s
  sourceRef:
    kind: GitRepository
    name: osdu-bicep
  path: ./software/apps/base/cert-manager
  prune: true
  validation: client
  # healthChecks:
  #   - kind: Deployment
  #     name: sealed-secrets
  #     namespace: kube-system